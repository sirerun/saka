# E3 -- Provider Plugin Registry

Acceptance: a new Provider can be registered and used from saka.json without editing saka.go's New or Config.Validate. Decision rationale: docs/adr/001-provider-plugin-registry.md.
fidelity: executable

- [x] T3.1 Design the provider registry API (Register/factory signature; init()-side-effect vs explicit-registration tradeoff) and record the chosen approach in docs/adr/001-provider-plugin-registry.md's Decision section  Owner: this session  Est: 1h  verifies: [UC-024]  deps: [T2.1]  acc: [docs/adr/001-provider-plugin-registry.md's Decision section names a concrete Register function signature, not TBD]  Done: 2026-08-29 (PR #30)
- [x] T3.2 Implement the registry (thread-safe Register/Lookup, duplicate-name rejection) per T3.1's design  Owner: this session  Est: 1h  verifies: [UC-024]  deps: [T3.1]  acc: [a unit test registers two providers under the same name and asserts the second call returns an error]  Done: 2026-08-29 (PR #32)
- [x] T3.3 Migrate provider/duckduckgo, provider/searxng, provider/startpage to self-register via the new convention  Owner: this session  Est: 1h  verifies: [UC-024]  deps: [T3.2]  acc: [all three built-in providers call types.Register from their own init(), and types.Lookup("duckduckgo"/"searxng"/"startpage") succeeds for each; saka.go's New/Config.Validate are untouched by this task -- removing their hardcoded switch is T3.4's job, sequenced after this one]  Done: 2026-08-29 (PR #35). Each provider's init() calls types.Register; saka.go untouched (verified via `gh pr diff --name-only`).  **Corrected 2026-08-29 (coordinator): the original acc line ("saka.go's New no longer contains a hardcoded switch") described T3.4's outcome, not something T3.3 alone -- scoped to provider/* -- can satisfy. Split cleanly: T3.3 makes providers self-register, T3.4 (now deps: [T3.2, T3.3], corrected below) makes saka.go actually use the registry.**
- [ ] T3.4 Update saka.New and Config.Validate to resolve providers through the registry instead of the hardcoded switch and name whitelist  Owner: TBD  Est: 1h  verifies: [UC-024]  deps: [T3.2, T3.3]  acc: [saka.go's New no longer contains a hardcoded switch over provider names; Config.Validate rejects an unregistered provider name with an error naming it unregistered; and go test ./... passes]  **Corrected 2026-08-29 (coordinator): added deps: T3.3 (providers must self-register before New can rely solely on the registry -- an empty registry with the switch removed would break all three built-ins) and pulled in the "switch removed" criterion from T3.3's corrected line above.**
- [ ] T3.5 Add unit tests for registry Register/Lookup, duplicate-name rejection, and the unknown-provider error path through Config.Validate  Owner: TBD  Est: 45m  verifies: [UC-024]  deps: [T3.2]  acc: [go test ./... -run TestRegistry passes and covers the unknown-provider path]
- [ ] T3.6 Update docs/SPEC.md section 3.3 ("Adding a provider") to document the registry convention  Owner: TBD  Est: 30m  verifies: [UC-024]  deps: [T3.4]  acc: [docs/SPEC.md section 3.3 shows a Register call, not "register in saka.New and Config.Validate"]
