# E5 -- Helm Chart Deploy

Acceptance: `helm install saka deploy/helm/saka` deploys the same saka+SearXNG stack as the plain kubectl-apply path, verified in CI.
fidelity: executable

- [x] T5.1 Scaffold a Helm chart under deploy/helm/saka templating the existing namespace/saka/searxng manifests  Owner: TBD  Est: 1.5h  verifies: [UC-026]  acc: [helm template deploy/helm/saka renders valid Kubernetes YAML for namespace, saka Deployment/Service, and searxng Deployment/Service]  **Done 2026-08-29 (PR #16, merged). Templates deploy/k8s/{namespace,saka,searxng}.yaml as a chart (Chart.yaml, values.yaml, templates/*); deploy/k8s/ untouched. helm lint and helm template both verified clean; helm template renders 7 resources (Namespace, 2 ConfigMaps, 2 Deployments, 2 Services). Executed via kazi (goal t5-1-helm-chart-scaffold, 11 predicates, converged iteration 1). Note for whoever picks up T5.2-T5.4: kazi's --parallel scheduler landed the actual commit as uncommitted collateral salvaged to a local-only ref (refs/kazi/salvage/...), not on the task/<goal-id> branch in the dispatched worktree -- had to recover it from a same-named local kazi-partition/* branch and cherry-pick onto a fresh branch off current main before pushing. See KAZI-EXEC.md's stuck-under-parallel guidance if this recurs.**
- [x] T5.2 Parameterize image repo/tag, resource limits, and SearXNG settings via values.yaml  Owner: TBD  Est: 1h  verifies: [UC-026]  deps: [T5.1]  acc: [helm template deploy/helm/saka --set image.tag=test renders the overridden tag into the saka Deployment]  **Done 2026-08-29 (PR #26). Flattened saka's image.*/resources.*/replicas/config from a nested `saka:` key to top-level values.yaml keys so `--set image.tag=test` and `--set resources.limits.cpu=2` override the saka Deployment as the acc line requires; searxng.* stays nested (bundled dependency, already fully parameterized by T5.1). Executed via kazi (goal t5-2-helm-values, 10 predicates, converged). Hit the same --parallel landmine T5.1 documented above: the actual commit landed on a scheduler-owned kazi-partition/* branch instead of task/t5-2-helm-values, recovered via cherry-pick; the recovered commit only half-flattened (left saka.replicas/saka.config nested), finished the refactor in a second commit before opening the PR.**
- [ ] T5.3 Add a CI job (or extend k8s-smoke) that runs `helm install` against the kind cluster and the existing deploy/smoke.sh  Owner: TBD  Est: 1h  verifies: [UC-026]  deps: [T5.1, T5.2]  acc: [.github/workflows/ci.yml's helm-smoke job passes on a fresh kind cluster]
- [x] T5.4 Update README.md's "Self-hosted stack" section to document the Helm install path alongside kubectl apply  Owner: TBD  Est: 30m  verifies: [UC-026]  deps: [T5.2]  acc: [README.md shows a helm install saka deploy/helm/saka command in the self-hosted section]  **Done 2026-08-29 (PR #44, merged). Added a "### Helm chart" subsection under "Self-hosted stack (Kubernetes)", additive alongside the existing kubectl/deploy/k8s/apply.sh path: build/load image, `helm install saka deploy/helm/saka --set image.repository=saka --set image.tag=local`, port-forward svc/saka, and the same deploy/smoke.sh verification the kubectl path uses, plus a matching production note. Executed via kazi (goal t5-4-readme-helm-doc, 9 predicates, converged iteration 2). Hit the same --parallel landmine T5.1/T5.2 documented above: the actual commit landed on a scheduler-owned kazi-partition/* branch instead of task/t5-4-readme-helm-doc, recovered via cherry-pick; content matched the predicate brief exactly, no rework needed.**
