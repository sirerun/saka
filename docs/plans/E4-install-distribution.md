# E4 -- Install & Release Distribution

Acceptance: the documented install paths (curl one-liner, Homebrew) work against real, owned infrastructure instead of placeholders.
fidelity: executable

- [x] T4.1 Decide the real install domain: own getsaka.dev, or repoint install.sh/README at github.com/sirerun/saka releases -- FOUNDER DECISION  Owner: David  Est: 15m  verifies: [UC-025]  kind: human  acc: [a decision is recorded in this task's completion note naming the chosen domain/URL]  **Decided 2026-08-29 (David, via AskUserQuestion in `/loop /apply --pool`): repoint install.sh/README at github.com/sirerun/saka releases. Do not buy/host getsaka.dev.**
- [x] T4.2 Create the sirerun/homebrew-tap GitHub repo with a Formula/saka.rb scaffold matching GoReleaser's brew section  Owner: David  Est: 1h  verifies: [UC-025]  kind: human  deps: [T4.1]  acc: [github.com/sirerun/homebrew-tap exists and contains Formula/saka.rb]  **Corrected 2026-08-29 (coordinator, verified against .goreleaser.yaml's `brews:` section): the repo is named `sirerun/homebrew-tap`, not `sirerun/tap`.**  **Done 2026-08-29 (coordinator). The repo already existed -- it's a shared tap already hosting other sirerun tools (serenity, mint, gist). No manual Formula/saka.rb scaffold was needed or written: this repo's existing formulas (e.g. Formula/serenity.rb) are GoReleaser-generated artifacts ("DO NOT EDIT" in their own header), so Formula/saka.rb is created automatically by T4.4's release, not hand-authored. Confirmed the org secret HOMEBREW_TAP_GITHUB_TOKEN existed but saka wasn't yet in its selected-repositories list; added it (`gh api -X PUT orgs/sirerun/actions/secrets/HOMEBREW_TAP_GITHUB_TOKEN/repositories/<saka-repo-id>`).**
- [x] T4.3 Update install.sh and README.md to match the domain/URL decided in T4.1  Owner: TBD  Est: 30m  verifies: [UC-025]  deps: [T4.1]  acc: [install.sh's curl target and README's install snippet both reference the decided URL, not getsaka.dev, unless T4.1 chose to own that domain]  **Done 2026-08-29 (PR #15, merged). Both files now curl `raw.githubusercontent.com/sirerun/saka/main/install.sh`; neither contains `getsaka.dev`. Executed via kazi (goal t4.3-install-url). Follow-up not fixed here: `.goreleaser.yaml`'s archive `name_template` is GoReleaser's version-less default, but install.sh's download step expects a version-qualified filename (`saka_${VERSION#v}_${os}_${arch}.tar.gz`) -- these won't match once a real release is cut; flagged for T4.4.**
- [x] T4.4 Cut a test release tag and verify GoReleaser publishes the Homebrew formula to sirerun/homebrew-tap and `brew install sirerun/tap/saka` succeeds  Owner: this session  Est: 1h  verifies: [UC-025]  deps: [T4.2, T4.3]  acc: [brew install sirerun/tap/saka installs a working saka binary on a clean machine/container]  **Done 2026-08-29 (tag v0.1.0). T4.3's flagged archive-name concern turned out to be a non-issue (GoReleaser's default archive name_template already includes the version). Found and fixed FIVE previously-uncaught bugs in this never-before-exercised release pipeline, one at a time via real failed CI runs (this repo had zero prior tags, so release.yml had never actually run): (1) ci.yml missing `workflow_call:` trigger, needed for release.yml's `uses: ./.github/workflows/ci.yml` (PR #50); (2) release.yml's setup-go pinned to Go 1.22, but go.mod requires >=1.25 (PR #51); (3) release.yml read the token from `secrets.HOMEBREW_TOKEN`, a secret that doesn't exist, instead of `secrets.HOMEBREW_TAP_GITHUB_TOKEN` (PR #51); (4) `.goreleaser.yaml`'s brews.repository never referenced the passed token via `token:`, so it would have silently fallen back to the repo-scoped GITHUB_TOKEN (PR #51, plus `version: 2` added to silence a schema warning); (5) the project claimed Apache-2.0 everywhere (README, goreleaser config, CLAUDE.md) but never shipped a LICENSE file -- added a verbatim, byte-diffed copy from apache.org (PR #52). A sixth issue was caught live and fixed without a PR gap: the first successful run published the formula to homebrew-tap's repo root (`saka.rb`) instead of `Formula/saka.rb` (no `directory:` set) -- confirmed broken with a real `brew install sirerun/tap/saka` failure ("No available formula"), fixed with `directory: Formula` (PR #53), and the stray root-level file deleted from homebrew-tap directly. Real end-to-end verification, not just CI: `brew untap`/`brew tap sirerun/tap`/`brew install sirerun/tap/saka` on this machine actually installed the binary (6 files, 7.9MB), and `saka search "test"` returned live results.**
