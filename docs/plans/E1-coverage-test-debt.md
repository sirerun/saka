# E1 -- Coverage & Test Debt Paydown

Acceptance: CI's coverage gate is raised from 40% to 55% and passes; SPEC.md's coverage claim matches the enforced gate. Decision rationale: docs/adr/002-coverage-gate-honesty.md.
fidelity: executable

- [x] T1.1 Reconcile SPEC.md section 11's "Coverage gate: >=70%" against ci.yml's actual 40% floor -- state the real floor and link docs/adr/002-coverage-gate-honesty.md  Owner: TBD  Est: 30m  verifies: [infrastructure]  acc: [docs/SPEC.md section 11 states the enforced gate percentage verbatim as it appears in .github/workflows/ci.yml]  Done: 2026-08-29  PR: #13
- [x] T1.2 Add table-driven tests for fetch/fetch.go's Fetch/FetchStream error paths (timeout, non-200, robots-disallowed, body-cap exceeded) -- currently 34.0% covered  Owner: TBD  Est: 1h  verifies: [infrastructure]  acc: [go test ./fetch/... -cover reports fetch package coverage >= 55%]  Done: 2026-08-29  PR: #23  **Coordinator note: this task's own plan-checkbox mark-done step was missed at the time -- code merged clean hours earlier, found and fixed during a full-plan sweep.** fetch package coverage 31.2% (measured at dispatch) -> 87.8%. Also found and fixed a real concurrency bug while writing FetchStream tests: `ExtractStream` unconditionally closed both `doneCh`/`errCh` on return even though each is written at most once, making the unwritten channel spuriously receive-ready with its zero value and racing the real send in the consumer's `select` (~50% of successful calls could get a `nil` error instead of the page). Fixed by removing the incorrect unconditional closes.
- [x] T1.3 Add tests for types/types.go's Page.Chunks and splitChunks edge cases (empty text, exact chunk-size boundary, multi-byte runes) -- currently 36.4% covered  Owner: TBD  Est: 45m  verifies: [infrastructure]  acc: [go test ./types/... -cover reports types package coverage >= 60%]  Done: 2026-08-29  PR: #21  **types package coverage 36.4% -> 100.0%. Executed via kazi (goal t1-3-types-coverage): survived a pre-dispatch kazi launcher crash (issue #1073 class, zero iterations lost, retried clean) then hit the same --parallel salvage gotcha T1.4/T5.1 documented (real commit landed on a scheduler-owned kazi-partition/* branch, not task/<goal-id>) -- recovered by cherry-picking onto a fresh branch off origin/main. The kazi-authored commit reached >=60% via WithDefaults/Chunks/RateLimitError.Error rather than the three splitChunks edge cases this task named, so a follow-up commit converted TestSplitChunks to table-driven with explicit empty/exact-boundary/multi-byte-rune cases asserting UTF-8 validity, rune counts, and exact reassembly.**
- [x] T1.4 Add tests for saka.go's Config.Validate edge cases and New()'s provider-construction error paths -- currently 30.3% covered  Owner: TBD  Est: 1h  verifies: [infrastructure]  acc: [go test . -cover reports root saka package coverage >= 55%]  Done: 2026-08-29  PR: #17  **Root package coverage 29.0% -> 95.7% (Validate 79.2% -> 100%, LoadConfig 0% -> 100%, homeDir 0% -> 100%, New 0% -> 89.3%). Executed via kazi (goal t1-4-saka-root-coverage): the --parallel scheduler hit the same stuck/salvage gotcha T5.1 documented (real commit landed on a scheduler-owned kazi-partition/* branch, not task/<goal-id>) -- recovered by cherry-picking onto a fresh branch off origin/main.**
- [ ] T1.5 Raise ci.yml's coverage gate from 40% to 55% and verify CI green at the new floor  Owner: TBD  Est: 30m  verifies: [infrastructure]  deps: [T1.2, T1.3, T1.4]  acc: [.github/workflows/ci.yml's coverage gate check compares against 55, and the next CI run on main passes it]
