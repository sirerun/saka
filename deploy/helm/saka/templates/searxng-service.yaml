apiVersion: v1
kind: Service
metadata:
  name: searxng
  namespace: {{ include "saka.namespace" . }}
  labels:
    app.kubernetes.io/name: searxng
    app.kubernetes.io/part-of: saka
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: searxng
  ports:
    - name: http
      port: 8080
      targetPort: http
