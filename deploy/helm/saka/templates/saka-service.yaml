apiVersion: v1
kind: Service
metadata:
  name: saka
  namespace: {{ include "saka.namespace" . }}
  labels:
    app.kubernetes.io/name: saka
    app.kubernetes.io/part-of: saka
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: saka
  ports:
    - name: http
      port: 8080
      targetPort: http
